class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        counts = defaultdict(int)

        for num in nums:
            counts[num] += 1
        
        for key, value in counts.items():
            if value != 2:
                return key
                
