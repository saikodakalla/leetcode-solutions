class Solution:
    def isPossibleToSplit(self, nums: List[int]) -> bool:
        counts = defaultdict(int) 

        for num in nums:
            counts[num] += 1
        
        for key, value in counts.items():
            if value > 2:
                return False
        
        return True
